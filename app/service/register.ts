
const Axios = require('axios').default;
import { Device } from '@nativescript/core';


export default class Register {
    sendPhoneNumber(phone: any) {
        
    }

    sendCodeActivation() {
        let uuid = Device.uuid;
    }
}